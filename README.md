# Task 01: Text Generation with GPT-2

This repository contains a text generation pipeline built using OpenAI's pre-trained **GPT-2** model via Hugging Face `transformers`.

## Project Structure
- `task1_gpt2.py`: Script to generate text using top-k and top-p sampling strategies.

## Requirements
```bash
pip install transformers torch
